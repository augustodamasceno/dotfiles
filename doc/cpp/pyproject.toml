[project]
name = "cpp-docs"
version = "0.1.0"
description = "C++ documentation pipeline using MkDocs Material, mkdoxy, and mkdocs-llmstxt"
requires-python = ">=3.10"
dependencies = [
    "mkdocs-material>=9.5",
    "mkdoxy>=1.2",
    "mkdocs-llmstxt>=0.1",
]

[tool.uv]
dev-dependencies = []
