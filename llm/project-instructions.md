# Project: <PROJECT_NAME>

## Overview
- **Description:** <Brief description of what this project does.>
- **Language(s):** <e.g., C++17, Python 3.12, C# 12>
- **Build System:** <e.g., CMake, Make, dotnet, Poetry>
- **Platform(s):** <e.g., Linux x86_64, Windows 11, macOS 14>

## Repository Structure
- `src/` — <description>
- `tests/` — <description>
- `docs/` — <description>
- `<other>/` — <description>

## Architecture & Key Decisions
- <Decision or constraint #1, e.g., "Uses a single-threaded event loop; do not introduce threads.">
- <Decision or constraint #2>

## Coding Standards
- Follow the global rules in `instructions.md`.
- **Additional project rules:**
  - <e.g., "All public API functions must have a unit test in tests/.">
  - <e.g., "No external dependencies unless approved.">

## Naming & File Conventions
- <e.g., "Feature branches: feature/<ticket-id>-short-description">
- <e.g., "Test files mirror source path: src/foo/bar.cpp → tests/foo/bar_test.cpp">

## Out of Scope
- <What the LLM should NOT do or change, e.g., "Do not modify the CMakeLists.txt root file.">
- <e.g., "Do not add logging frameworks; use the existing Logger class.">
